#include <stdio.h>

int main() {
	int x[i], i;
	
	for(i=0;i<5;i++) {
		scanf("%d", &x[i]);
}

    if((x[0]==1||x[1]==1)&&(x[2]==1&&x[3]==1)&&x[4]==1)
    printf("APROVADO");
    
    else
    printf("0");
    
	return 0;
}
