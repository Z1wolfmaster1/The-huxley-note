#include <stdio.h>
#include <math.h>

int main() {
	const double pi=3.14159;
	double a, r1, r2;
	
	scanf("%lf", &r1);
	r2=r1/100;
	printf("Area = %.4f", a, a=pi*(pow(r2,2)));
	
	
	return 0;
}
